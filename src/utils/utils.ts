export function parse(value: string): string {
  return value + '-1'
}
export function parse2(value: string): string {
  return value + '-1'
}

export function parse3(value: string): string {
  return value + '-1'
}
export function parse4(value: string): string {
  return value + '-1'
}
